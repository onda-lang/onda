[package]
name = "onda_cpal"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
cpal = { version = "0.18.2", features = ["pipewire", "realtime"] }
libc = "0.2"
onda_realtime = { path = "../onda_realtime" }

[lib]
path = "src/lib.rs"
