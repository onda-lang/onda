namespace std::filter:
  proc OnePole<T>:
    ins<T> 1
    outs<T> 1

    params<T>:
      cutoff = 1000.0

    init<T>:
      z = 0.0

    block:
      a: T = T(1.0) - exp(-T(TWO_PI) * cutoff / T(SR))
      sample:
        z = z + a * (in1 - z)
        out1 = z
