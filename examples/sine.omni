params:
  freq = 440.0 { 20, 1000 }

init:
  phase = 0.0

block:
  tau = f32(TWO_PI)
  incr = freq * tau / SR
  
  sample:
    phase = phase + incr
    if (phase > tau):
      phase = phase - tau
    out1 = sin(phase)
