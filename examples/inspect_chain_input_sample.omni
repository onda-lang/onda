proc Source {
  ins { in1 }
  outs { out1 }
  sample {
    out1 = in1 + 1.0
  }
}

proc Gain {
  ins { in1 }
  outs { out1 }
  sample {
    out1 = in1 * 2.0
  }
}

ins { in1 }
outs { out1 }

init {
  src = Source()
  gain = Gain()
}

sample {
  src(in1)
  gain(src.out1)
  out1 = gain.out1
}
