params:
  step = 0.005

init:
  phase = 0.0
  value = 0.0

block:
  phase = phase + step * BLOCK_SIZE
  if (phase > TWO_PI):
    phase = phase - TWO_PI
  value = sin(phase)

  sample:
    out1 = value
