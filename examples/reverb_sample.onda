import std/osc
import std/env
import reverb

init:
    reverb = Reverb()
    osc = std::osc::Saw(freq = 100.0)
    env = std::env::DecayEnv(
        decay_s = 0.05,
        trigger = 1.0,
    )

sample:
    sig = osc() * env()
    reverb(sig, sig)
    out1 = reverb.out1
    out2 = reverb.out2
